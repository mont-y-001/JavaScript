let name = "Mohit"
let repoCount = 50;

console.log(`Hii,This sie ${name},my Repo Count is ${repoCount}`) //important syntax

// const newName = new String("Monty");

// console.log(newName.__proto__);
// console.log(newName[0]);
// console.log(newName.length);

//****toUpperCase,charAt,indexOf
//***trim = remove empty spaces from text
//***slice = type of substring but can take -ve value as well and substring
//***replce,include = to check something is written or not 
//***split = used to split by some letter or kaywords to some text
const newGame = "harshit"
console.log(newGame)

const anotherGame = newGame.slice(-8, 4)
console.log(anotherGame);
